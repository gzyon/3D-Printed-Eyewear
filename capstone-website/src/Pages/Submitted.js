import { Typography } from "@mui/material";
import React from "react";

const Submitted = (props) => {
    return (
        <Typography alignContent={"center"} justifyContent={"center"} align="center">
            Your frame has been sent for submission!
        </Typography>
    )
}

export default Submitted;